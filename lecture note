$ git init : 버전 관리 선언

# 개행 문자(Newline) 설정
## Windows
$ git config --global core.autocrlf true

# 사용자 정보
## 커밋(버전 생성)을 위한 정보 등록 (github에서의 이름, github 가입 시 이메일 주소를 사용)
$ git config --global user.name 'YOUR_NAME'      
$ git config --global user.email 'YOUR_EMAIL'

# 구성 확인 (정상적으로 정보가 등록되었는 지 확인)
$ git config --global --list

# 프로젝트 버전 관리 상태 확인 (프로젝트 구조 확인)
$ git status

# 모든 파일들에 대해 버전 관리를 한
$ git add .

# 메시지(-m)와 함께 버전을 생성.
$ git commit -m 'Start project'

# commit으로 등록한 버전 확인 (어떤 commit이 만들어 졌는지 확인)
$ git log

# remote : 원격(github의 저장소를 의미) origin : 주소의 이름 
## https://github.com/yoojanghun/starbucks.git : 주소
$ git remote add origin https://github.com/yoojanghun/starbucks.git

# 원격 저장소에 push. 원격 저장소 이름은 origin. master라는 branch에 업로드
$ git push origin master
